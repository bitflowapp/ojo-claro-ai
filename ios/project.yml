name: OjoClaroIOS
options:
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: com.ojoclaro.ios
    SWIFT_VERSION: 5.9
targets:
  OjoClaroIOS:
    type: application
    platform: iOS
    sources:
      - OjoClaroIOS
    info:
      path: OjoClaroIOS/Info.plist
    settings:
      base:
        INFOPLIST_FILE: OjoClaroIOS/Info.plist
