<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-feature
    android:name="android.hardware.camera.any"
    android:required="false" />

  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
  <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
  <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

  <queries>
    <package android:name="com.whatsapp" />
    <package android:name="com.whatsapp.w4b" />
    <package android:name="com.google.android.apps.maps" />
    <package android:name="com.ubercab" />
    <package android:name="com.cabify.rider" />
    <package android:name="com.didiglobal.passenger" />
    <package android:name="org.telegram.messenger" />
    <package android:name="com.google.android.apps.messaging" />
    <package android:name="com.android.settings" />
    <package android:name="com.android.chrome" />
    <package android:name="com.google.android.dialer" />
    <package android:name="com.android.dialer" />
    <package android:name="com.mercadopago.wallet" />
    <package android:name="com.galicia.mobile" />
    <package android:name="ar.com.santander.rio.mbanking" />
    <package android:name="com.bbva.nxt_argentina" />
    <package android:name="com.icbc.mobile.abroad" />
    <package android:name="ar.com.bna" />
    <package android:name="ar.com.bapro.bip" />

    <intent>
      <action android:name="android.intent.action.SEND" />
      <data android:mimeType="text/plain" />
    </intent>

    <intent>
      <action android:name="android.intent.action.VIEW" />
      <data android:scheme="geo" />
    </intent>

    <intent>
      <action android:name="android.speech.RecognitionService" />
    </intent>
  </queries>

  <application
    android:allowBackup="false"
    android:networkSecurityConfig="@xml/network_security_config"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.OjoClaro">

    <activity
      android:name=".MainActivity"
      android:exported="true"
      android:launchMode="singleTop"
      android:screenOrientation="portrait">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <intent-filter>
        <action android:name="com.ojoclaro.android.ACTION_START_LISTENING" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>

    </activity>

    <service
      android:name=".accessibility.OjoClaroAccessibilityService"
      android:exported="true"
      android:label="@string/app_name"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">

      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>

      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/ojo_claro_accessibility_service" />

    </service>

    <service
      android:name=".voice.OjoClaroQuickTileService"
      android:exported="true"
      android:icon="@drawable/ic_quick_tile_mic"
      android:label="@string/quick_tile_label"
      android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">

      <intent-filter>
        <action android:name="android.service.quicksettings.action.QS_TILE" />
      </intent-filter>

      <meta-data
        android:name="android.service.quicksettings.ACTIVE_TILE"
        android:value="false" />

    </service>

    <!-- V1.13 Camera Assist: el tipo camera se declara acá pero solo se
         activa en startForeground mientras la cámara de Estela está abierta
         (mismo patrón on-demand que Outdoor Describir). -->
    <service
      android:name=".global.GlobalAssistantService"
      android:exported="false"
      android:foregroundServiceType="microphone|camera" />

    <!-- Outdoor Guidance v1: navegación peatonal con notificación visible.
         Se inicia SOLO por interacción explícita del usuario. Sin
         ACCESS_BACKGROUND_LOCATION: el foreground service cubre la guía.
         Android 14+: la captura bajo demanda exige el tipo "camera" además
         de "location"; el tipo camera se activa SOLO en la acción Describir. -->
    <service
      android:name=".outdoor.OutdoorForegroundService"
      android:exported="false"
      android:foregroundServiceType="location|camera" />

    <!-- WA-3: listener de notificaciones EXCLUSIVO de WhatsApp. Solo LEE; nunca
         responde ni cancela. exported="true" es obligatorio porque el SO se
         vincula al servicio; android:permission garantiza que solo el sistema
         pueda hacerlo. El acceso lo concede el usuario en Ajustes ("Acceso a
         notificaciones"): NO es un uses-permission. El filtrado "solo WhatsApp"
         se hace en código (OjoClaroWhatsAppNotificationListenerService). -->
    <service
      android:name=".notifications.OjoClaroWhatsAppNotificationListenerService"
      android:exported="true"
      android:label="@string/app_name"
      android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">

      <intent-filter>
        <action android:name="android.service.notification.NotificationListenerService" />
      </intent-filter>

    </service>

  </application>

</manifest>
